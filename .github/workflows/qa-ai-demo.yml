name: QA AI Demo - Automated Testing

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Manual trigger

jobs:
  deploy-and-test:
    runs-on: ubuntu-latest
    
    steps:
    # Step 1: Checkout the demo web app
    - name: Checkout Demo Web App
      uses: actions/checkout@v4
      
    # Step 2: Setup simple HTTP server
    - name: Setup Python HTTP Server
      run: |
        python3 -m http.server 8080 &
        echo "HTTP_SERVER_PID=$!" >> $GITHUB_ENV
        sleep 5  # Wait for server to start
        
    # Step 3: Verify app is running
    - name: Verify App is Running
      run: |
        curl -f http://localhost:8080 || exit 1
        echo "✅ Demo app is running on http://localhost:8080"
        
    # Step 4: Checkout QA AI Action
    - name: Checkout QA AI Action
      uses: actions/checkout@v4
      with:
        repository: floor-licker/qa-ai
        token: ${{ secrets.GITHUB_TOKEN }}
        path: qa-ai-action
        
    # Step 5: Run QA AI Testing (Local Docker Action)
    - name: Run QA AI Analysis
      uses: ./qa-ai-action
      with:
        app_url: 'http://localhost:8080'
        max_depth: 4
        timeout: 300
        frameworks: 'playwright,cypress'
        openai_api_key: ${{ secrets.OPENAI_API_KEY }}
        
    # Step 6: Upload Results
    - name: Upload QA AI Results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: qa-ai-test-results
        path: qa-ai-tests/
        
    # Step 7: Display Results Summary
    - name: Display QA Results Summary
      if: always()
      run: |
        echo "🎉 QA AI testing completed!"
        if [ -d "qa-ai-tests" ]; then
          echo "📁 Generated test files:"
          find qa-ai-tests -name "*.spec.ts" -o -name "*.cy.js" | head -10
          echo ""
          echo "📊 Total test files: $(find qa-ai-tests -name "*.spec.ts" -o -name "*.cy.js" | wc -l)"
        else
          echo "ℹ️ No test files found in qa-ai-tests directory"
        fi
        
    # Step 8: Cleanup
    - name: Cleanup
      if: always()
      run: |
        if [ ! -z "$HTTP_SERVER_PID" ]; then
          kill $HTTP_SERVER_PID || echo "Server already stopped"
        fi 